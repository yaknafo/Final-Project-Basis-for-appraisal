@model BasisForAppraisal_finalProject.ViewModel.FormViewModel
@{
    Layout = null;
}

<div dir="rtl" style="border-bottom: solid;">
    <br />
    <br />
    <div draggable="true">
        <div dir="rtl">
           
            <h4>שאלה</h4>
            @Html.TextAreaFor(x => x.NewQuestionMultipleChoice.Title, new { @cols = 90, @rows = 2 })
        </div>
        @if (Model.NewQuestionMultipleChoice.QuestionId > 0)
        {
            @Html.ActionLink("ערוך", "upload_excelfile", new { @path = "rrr" })


            @*@Html.ActionLink("מחק", "deleteQustion", new { @formID = Model.FormId, @quesNumber = Model.QuestionId })*@
            <table>
                <tr>
                    <th>delete</th>
                    <th>  @Html.CheckBoxFor(x => x.NewQuestionMultipleChoice.deleteQuestion, new { style = "height: 25px;  width:25px;" })</th>
                </tr>
            </table>
        }

        <br />
        <br />
        @*@Html.RadioButtonFor(a => a.selectedAnswer, Model.Answers[0].AnswerId, new { style = "height: 20px;  width:20px; margin-top: 10px; margin-left: 50px;" })*@
        @for (int i = 0; i < @Model.NewQuestionMultipleChoice.Answers.Count; i++)
        {
            @Html.TextBoxFor(x => x.NewQuestionMultipleChoice.Answers[i].Text)
            <br />
            <br />
        }
        @*@Ajax.ActionLink("change", "AddAnswerToQuestion", new { id =1, q=Model }, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "MultipleChoice", InsertionMode = InsertionMode.InsertAfter })*@
        <button type="submit" class="btn btn-primary" name="submit" value="AddAnswerToMulitiChoice" style="margin-right:40%">+</button>



    </div>